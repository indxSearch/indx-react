export enum SystemState {
  Hibernated = -1,
  Created = 0,
  Loading = 1,
  Loaded = 2,
  Indexing = 3,
  Ready = 4,
  Error = 255
}

export enum BoostStrength {
  Low = 1,
  Medium = 2,
  High = 3
}
