---
"@indxsearch/systm": minor
---

NavigationMenu: `size="large"` (matches Button large, 21px icons), `align="end"` to pin the list and panel to the end edge, `panelWidth`, vertical orientation for mobile menus, and an `icon` prop on `NavigationMenuLink` so a top-level link matches a trigger beside it.
