---
"@indxsearch/systm": minor
---

Breadcrumbs: `overflow="truncate"` keeps the trail on one line for headers. Labels ellipsize, ancestors give way before the current page, and a step with an icon collapses to the icon and its switcher. The default stays `wrap`.
