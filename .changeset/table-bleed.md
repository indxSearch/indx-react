---
"@indxsearch/systm": minor
---

Table: with `scrollable`, `bleedEnd` lets the frame reach into the gutter on its end side, so a table that has to scroll runs to the edge of the screen. The start side stays at the page margin, where a pinned first column belongs.
